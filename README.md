# Лабораторная работа: REST API на Python + Litestar + PostgreSQL

# Описание проекта

Асинхронный REST API сервис с архитектурой N-уровней (Controller → Service → Repository), реализованный на Python 3.13 с использованием фреймворка Litestar и SQLAlchemy 2.0. Проект включает полную контейнеризацию, систему контроля качества кода и автоматическое развёртывание.

# Архитектура

Модель данных: ORM-модель User (id, name, email, created_at)
Паттерны: Repository (доступ к данным) + Service (бизнес-логика) + Controller (HTTP-слой)
Внедрение зависимостей: Нативная DI через Litestar
Асинхронность: Полностью асинхронный стек (async/await)
# Технологический стек

Технология	Назначение
Python 3.13	Основной язык программирования
Litestar	Асинхронный веб-фреймворк
SQLAlchemy 2.0	ORM для работы с базой данных
asyncpg	Асинхронный драйвер PostgreSQL
PostgreSQL 16	Реляционная база данных
Docker + Docker Compose	Контейнеризация и оркестрация
pgAdmin 4	Веб-интерфейс для управления БД
Black + isort + pylint	Контроль качества кода
pre-commit	Автоматические проверки перед коммитом
# Функциональность API

Основной эндпоинт

http
GET /users
Параметры запроса:

page - номер страницы (по умолчанию: 1)
count - количество записей на странице (по умолчанию: 10)
Пример ответа:

json
{
  "items": [
    {
      "id": 1,
      "name": "John Smith",
      "email": "johnsmith1@company.com"
    }
  ],
  "total": 100
}
Дополнительные эндпоинты

GET /docs - Swagger документация API
GET /schema - OpenAPI схема
GET /health - Проверка работоспособности
# Запуск проекта через Docker

1. Предварительная подготовка

bash
# Даем права на выполнение скрипта запуска
chmod +x entrypoint.sh
2. Сборка и запуск

bash
# Запуск всех сервисов в фоновом режиме
docker compose up -d --build
3. Доступ к сервисам

Сервис	URL	Описание	Учетные данные
API	http://localhost:8000/users	Основной эндпоинт	-
Swagger	http://localhost:8000/docs	Документация API	-
pgAdmin	http://localhost:5050	Управление базой данных	admin@example.com / admin
PostgreSQL	localhost:5432	База данных	postgres / postgres
4. Остановка

bash
# Остановка всех сервисов с удалением контейнеров
docker compose down

# Остановка с удалением томов (данные БД будут удалены!)
docker compose down -v
# Наполнение тестовой базы данных

Автоматическое наполнение (100 пользователей)

bash
docker compose exec db psql -U postgres -d test_db -c "
INSERT INTO users (name, email) 
SELECT 
  CASE 
    WHEN id % 10 = 1 THEN 'John Smith'
    WHEN id % 10 = 2 THEN 'Sarah Johnson'
    WHEN id % 10 = 3 THEN 'Mike Brown'
    WHEN id % 10 = 4 THEN 'Emily Davis'
    WHEN id % 10 = 5 THEN 'David Wilson'
    WHEN id % 10 = 6 THEN 'Lisa Anderson'
    WHEN id % 10 = 7 THEN 'James Taylor'
    WHEN id % 10 = 8 THEN 'Anna Martinez'
    WHEN id % 10 = 9 THEN 'Robert Thomas'
    ELSE 'Jennifer Lee'
  END,
  LOWER(SPLIT_PART(name, ' ', 1) || SPLIT_PART(name, ' ', 2) || id || '@company.com')
FROM generate_series(1, 100);
"
Проверка данных

bash
# Подсчет пользователей
docker compose exec db psql -U postgres -d test_db -c "SELECT COUNT(*) FROM users;"

# Просмотр первых 10 записей
docker compose exec db psql -U postgres -d test_db -c "SELECT * FROM users LIMIT 10;"
🔧 Контроль качества кода

Установка pre-commit хуков

bash
# Установка хуков
pre-commit install

# Запуск проверок для всех файлов
pre-commit run --all-files
Ручной запуск проверок

bash
# Автоматическое форматирование кода
black . && isort .

# Статический анализ
pylint app/

# Проверка типов (если используется mypy)
mypy app/
Конфигурация инструментов

Проект настроен с использованием:

Black: Форматирование Python кода
isort: Сортировка импортов
pylint: Статический анализ кода
pre-commit: Автоматический запуск проверок
📡 Тестирование API

Примеры запросов через curl

bash
# Получение всех пользователей (пагинация по умолчанию)
curl http://localhost:8000/users

# Пагинация: страница 2, 5 записей на странице
curl "http://localhost:8000/users?count=5&page=2"

# Пагинация: страница 3, 20 записей на странице
curl "http://localhost:8000/users?count=20&page=3"
Примеры запросов через Python

python
import httpx

async def test_api():
    async with httpx.AsyncClient(base_url="http://localhost:8000") as client:
        # Получение первой страницы
        response = await client.get("/users", params={"count": 5, "page": 1})
        data = response.json()
        print(f"Всего пользователей: {data['total']}")
        print(f"На странице: {len(data['items'])}")
# Структура проекта

text
.
├── app/
│   ├── controllers/      # HTTP контроллеры
│   ├── services/         # Бизнес-логика
│   ├── repositories/     # Работа с данными
│   ├── models/          # ORM модели
│   └── main.py          # Точка входа
├── migrations/          # Миграции базы данных
├── docker-compose.yml   # Конфигурация Docker Compose
├── Dockerfile          # Конфигурация Docker образа
├── entrypoint.sh       # Скрипт запуска приложения
├── requirements.txt    # Зависимости Python
├── pyproject.toml     # Конфигурация инструментов
├── .pre-commit-config.yaml # Конфигурация pre-commit
└── README.md           # Документация
# Работа с миграциями

bash
# Создание новой миграции
docker compose exec web alembic revision --autogenerate -m "Описание изменений"

# Применение миграций
docker compose exec web alembic upgrade head

# Откат миграции
docker compose exec web alembic downgrade -1
# Отладка и логирование

bash
# Просмотр логов приложения
docker compose logs -f web

# Просмотр логов базы данных
docker compose logs -f db

# Вход в контейнер приложения
docker compose exec web bash

# Проверка статуса контейнеров
docker compose ps
